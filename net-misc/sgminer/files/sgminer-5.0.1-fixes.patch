Index: sgminer-5.0.1/sgminer.c
===================================================================
--- sgminer-5.0.1.orig/sgminer.c
+++ sgminer-5.0.1/sgminer.c
@@ -8275,6 +8275,7 @@ int main(int argc, char *argv[])
   /* sgminer_path is current dir */
   sgminer_path = (char *)alloca(PATH_MAX);
 #ifndef _MSC_VER
+  char *s;
   s = strdup(argv[0]);
   strcpy(sgminer_path, dirname(s));
   free(s);
